<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
        <!-- BOOTSTRAP ICONS -->
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
        <!-- TAILWINDCSS -->
        <link rel="stylesheet" href="/css/output.css">
</head>
<body>
    <style>
        #listaProdutosCarrinho {
            max-height: calc(100vh - 339px);
            overflow-y: auto; 
        }
    </style>
    <section id="carrinho" class=" m-auto border   w-[90%] z-50 duration-300  bg-white flex flex-col justify-between max-w-screen-md ">
        <div class="text-right text-gray-700 text-xl flex flex-col">
            <h2 class="w-full p-4 text-2xl text-center">LISTA DE PRODUTOS</h2>

            <!-- LISTA DE PRODUTOS NO CARRINHO -->
            <ul id="listaProdutosCarrinho" class="overflow-y-auto h-screen border text-xl text-left p-2">
                <% if (typeof itensProcessados.itens != 'undefined' && itensProcessados.itens) { %>                 
                    <% itensProcessados.itens.forEach(e => { %>
                        <li class="border-b-2 border-gray-500 text-gray-700 flex items-center justify-between border-dashed gap-2 ">
                            <span class="  text-left text-sm">
                                <%= e.nome_produto %>
                            </span> 

                            <% let totalUnidade = Number(e.preco) *  Number(e.qtdProduto)  %>
                            <span class="text-right text-md w-24  flex-none text-sm ">
                                
                                R$ <%= e.preco %> x  <%= e.qtdProduto %> <br>
                                = R$ <%= totalUnidade %>
                            </span>
                        </li>
                    <% }) %>
                <% } %>
            </ul>
        </div>

        <div>
            <div class="flex justify-between items-center font-semibold">
                <h2 class="p-4 text-xl " >TOTAL: </h2>
                <h2 class="p-4 text-xl " id="subtotal">R$ <%= itensProcessados.total  %></h2>
            </div>
            <% const valorTotalAPagar = Number(itensProcessados.total.replace(',', '.')) %>
            <button onclick="rerarPixQrCode(this, '<%= valorTotalAPagar%>')" class="bg-blue-500 px-4 py-2 w-full font-semibold text-white my-2 flex items-center justify-center space-x-4">
                <img src="/icons/logo-pix-520x520.png" alt="" class="w-10">
                <p class="text-xl leading-none ">PAGAR COM PIX</p>
            </button>
        </div>
    </section>

    <div id="sectionPix" class="hidden border fixed top-1/2 left-1/2 p-10 bg-gray-50 transform -translate-x-1/2 -translate-y-1/2 space-y-5 shadow-lg w-full ">
        <button onclick="fecharSectionPix()" class="absolute top-2 right-3">
            <svg xmlns="http://www.w3.org/2000/svg" width="25" height="25" fill="currentColor" class="bi bi-x-lg text-red-500" viewBox="0 0 16 16">
                <path d="M2.146 2.854a.5.5 0 1 1 .708-.708L8 7.293l5.146-5.147a.5.5 0 0 1 .708.708L8.707 8l5.147 5.146a.5.5 0 0 1-.708.708L8 8.707l-5.146 5.147a.5.5 0 0 1-.708-.708L7.293 8z"/>
            </svg>
        </button>

       <img src="" alt=""  id="qrCode" class="m-auto p-4 bg-white rounded-lg">
       <button class="bg-blue-500 font-semibold text-white  w-52 flex items-center justify-center space-x-2 px-4 py-2 m-auto">
           <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-clipboard" viewBox="0 0 16 16">
               <path d="M4 1.5H3a2 2 0 0 0-2 2V14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V3.5a2 2 0 0 0-2-2h-1v1h1a1 1 0 0 1 1 1V14a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V3.5a1 1 0 0 1 1-1h1z"/>
               <path d="M9.5 1a.5.5 0 0 1 .5.5v1a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5v-1a.5.5 0 0 1 .5-.5zm-3-1A1.5 1.5 0 0 0 5 1.5v1A1.5 1.5 0 0 0 6.5 4h3A1.5 1.5 0 0 0 11 2.5v-1A1.5 1.5 0 0 0 9.5 0z"/>
           </svg>
            <p>COPIAR PIX</p>
    </button>
    </div>
</body>
</html>

<script src="/js/loja/checkout.js"></script>