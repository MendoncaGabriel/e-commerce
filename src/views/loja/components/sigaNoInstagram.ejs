
<% if (typeof data != "undefined" && data ) { %>
 
    <section class="w-full aspect-video md:aspect-auto md:py-16 p-5 flex flex-col justify-center items-center space-y-2 md:space-y-4">
        <div class="flex items-center justify-center space-x-2 text-2xl font-semibold">
            <i class="bi bi-instagram"></i>
            <p>shopdosbaloes</p>
        </div>
        <p >Estamos no instagram</p>
    
        <a href="<%= data %>" target="_blank" class="px-4 py-2  rounded-full   bg-blue-400 text-white ">SIGA-NOS</a>
    </section>
<% } %>